server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: probamos-paginacion
          uri: https://github.com/anpeni/tfg-sobremesa-final
          search-paths: config-data
  application:
    name: config-service
