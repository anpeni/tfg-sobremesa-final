server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: presentacion-03
          uri: https://github.com/anpeni/tfg-sobremesa-final
          search-paths: config-data
  application:
    name: config-service
