server:
  port: 8003
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: ${EUREKA_SERVICE_URI:http://eureka-service:8761/eureka}
    instance:
      hostname: localhost